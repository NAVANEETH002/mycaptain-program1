#include<iostream.h>
#include<conio.h>
using namespace std;
int main()
{
  float a,b,c;
  clrscr();
  cout<<"Enter the two numbers:"<<endl:
  cin>>a>>b;
  c=a+b;
  cout<<"Produt = "<<c;
  return 0;
}
